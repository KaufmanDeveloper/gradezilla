{{#nrg-list selectionType="single" items=assignments select="select" loading=loading as |list|}}
{{#list.items as |item|}}
  <div class="content">
    <a class = "header">{{item.name}}</a>
  </div>
{{/list.items}}
{{/nrg-list}}
